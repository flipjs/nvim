Install the following helpers GLOBALLY (google search on how-to):


* ACK
    OSX:
      brew install ack
    Linux:
      apt-get update
      apt-get install ack-grep
* AG
    OSX
      brew install the_silver_searcher
    Linux
      apt-get update
      apt-get install silversearcher-ag

* CTAGS
- Install universal ctags
  - steps:
      ```
      git clone https://github.com/universal-ctags/ctags.git
      cd ctags
      ./autogen.sh
      ./configure
      make
      sudo make install
      ```

* JSCTAGS
    npm install -g git+https://github.com/ramitos/jsctags.git

* POWERLINE FONTS
    git clone https://github.com/powerline/fonts.git
    run ./install.sh

NPM Packages: (npm install -g)

* bower
* gulp
* grunt-cli
* webpack
* webpack-dev-server
* js-beautify
* jshint
* jscs
* standard
* standard-format
* eslint
* babel-cli
* node-gyp
* http-server
* typescript
* jq-cli-wrapper
* json-server
* commitizen
* jsdoc
* elm
* elm-test
* elm-oracle
* prettier
* tern

COPY PASTE THIS:
npm install -g bower gulp grunt-cli webpack webpack-dev-server js-beautify jshint jscs standard standard-format eslint babel-cli node-gyp http-server typescript jq-cli-wrapper json-server commitizen jsdoc elm elm-test elm-oracle prettier tern

